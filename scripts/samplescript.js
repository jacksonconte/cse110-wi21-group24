/**
 * @method foobar
 * @description Does a random thing
 * @param {Number} x
 * @param {Number} y
 * @returns The sum of x and y
 */
function foobar(x, y) {
    console.log(x + y);
    return x + y;
}

foobar(4, 5);
